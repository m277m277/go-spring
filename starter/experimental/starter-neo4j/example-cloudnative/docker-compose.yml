services:
  neo4j:
    image: neo4j:5
    container_name: starter-neo4j-cloudnative
    environment:
      # The credentials used by conf/app.properties (neo4j / password).
      NEO4J_AUTH: neo4j/password
    ports:
      - "127.0.0.1:7687:7687"
